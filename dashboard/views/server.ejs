<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= guild.name %> - Oblivion Bot</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
            background: #f2f2f7;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }

        nav {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            color: #1d1d1f;
            padding: 0.75rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 0.5px solid rgba(0, 0, 0, 0.05);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        nav h1 {
            font-size: 1.25rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: #1d1d1f;
        }

        nav .nav-links {
            display: flex;
            gap: 0.5rem;
        }

        nav a {
            color: #007aff;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 12px;
            transition: all 0.2s ease;
            background: rgba(0, 122, 255, 0.1);
            font-size: 0.9rem;
            font-weight: 500;
        }

        nav a:hover {
            background: rgba(0, 122, 255, 0.2);
        }

        nav a:active {
            transform: scale(0.96);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 1.5rem;
        }

        .server-header {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            padding: 1.5rem;
            border-radius: 20px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
        }

        .server-header img {
            width: 64px;
            height: 64px;
            border-radius: 16px;
        }

        .server-header h2 {
            color: #1d1d1f;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 0.25rem;
        }

        .server-header p {
            color: #86868b;
            font-size: 0.9rem;
        }

        .server-info {
            display: flex;
            gap: 0.75rem;
            flex-wrap: wrap;
            margin-top: 0.5rem;
        }

        .info-badge {
            background: rgba(0, 0, 0, 0.05);
            color: #1d1d1f;
            padding: 0.35rem 0.75rem;
            border-radius: 8px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            padding: 1.25rem;
            border-radius: 16px;
            text-align: center;
            transition: all 0.2s ease;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
        }

        .stat-card:active {
            transform: scale(0.98);
        }

        .stat-card h3 {
            font-size: 2rem;
            color: #007aff;
            margin-bottom: 0.25rem;
            font-weight: 600;
        }

        .stat-card p {
            color: #86868b;
            font-weight: 500;
            font-size: 0.8rem;
        }

        .stat-card .icon {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .settings-section, .cases-section {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            padding: 1.5rem;
            border-radius: 20px;
            margin-bottom: 1.5rem;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
        }

        .settings-section h2, .cases-section h2 {
            color: #1d1d1f;
            margin-bottom: 1.25rem;
            font-size: 1.25rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 0.5px solid rgba(0, 0, 0, 0.08);
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: #1d1d1f;
            font-weight: 500;
            font-size: 0.9rem;
        }

        .form-group select {
            width: 100%;
            padding: 0.75rem;
            border: none;
            border-radius: 12px;
            font-size: 0.95rem;
            background: rgba(0, 0, 0, 0.04);
            color: #1d1d1f;
            cursor: pointer;
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
            transition: all 0.2s ease;
        }

        .form-group select:hover {
            background: rgba(0, 0, 0, 0.06);
        }

        .form-group select:focus {
            outline: none;
            background: rgba(0, 122, 255, 0.08);
        }

        .checkbox-group {
            display: flex;
            gap: 0.75rem;
            flex-wrap: wrap;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(0, 0, 0, 0.03);
            padding: 0.75rem 1rem;
            border-radius: 12px;
            transition: all 0.2s ease;
            cursor: pointer;
        }

        .checkbox-item:hover {
            background: rgba(0, 0, 0, 0.05);
        }

        .checkbox-item:active {
            transform: scale(0.98);
        }

        .checkbox-item input {
            width: 20px;
            height: 20px;
            cursor: pointer;
            accent-color: #007aff;
        }

        .checkbox-item label {
            cursor: pointer;
            margin: 0;
            color: #1d1d1f;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .btn {
            background: #007aff;
            color: white;
            padding: 0.85rem 2rem;
            border: none;
            border-radius: 12px;
            font-size: 0.95rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
            box-shadow: 0 2px 8px rgba(0, 122, 255, 0.3);
        }

        .btn:hover {
            background: #0051d5;
        }

        .btn:active {
            transform: scale(0.96);
        }

        .case-item {
            padding: 1rem;
            border-radius: 12px;
            background: rgba(0, 0, 0, 0.03);
            margin-bottom: 0.75rem;
            transition: all 0.2s ease;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
        }

        .case-item:active {
            transform: scale(0.99);
        }

        .case-item h4 {
            color: #1d1d1f;
            margin-bottom: 0.5rem;
            font-size: 1rem;
            font-weight: 600;
        }

        .case-item p {
            color: #86868b;
            font-size: 0.85rem;
            margin-bottom: 0.35rem;
        }

        .case-item p strong {
            color: #1d1d1f;
            font-weight: 600;
        }

        .success-message {
            background: rgba(52, 199, 89, 0.1);
            color: #30be71;
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1rem;
            display: none;
            border: 0.5px solid rgba(52, 199, 89, 0.2);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .error-message {
            background: rgba(255, 59, 48, 0.1);
            color: #ff3b30;
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1rem;
            display: none;
            border: 0.5px solid rgba(255, 59, 48, 0.2);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .quick-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 0.75rem;
            margin-bottom: 1.5rem;
        }

        .info-card {
            background: rgba(0, 0, 0, 0.03);
            padding: 1rem;
            border-radius: 12px;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
        }

        .info-card h4 {
            color: #86868b;
            font-size: 0.75rem;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
            letter-spacing: 0.5px;
            font-weight: 600;
        }

        .info-card p {
            color: #1d1d1f;
            font-size: 1.1rem;
            font-weight: 600;
        }

        /* Tab Navigation */
        .tab-navigation {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            padding: 0.5rem;
            border-radius: 16px;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
        }

        .tab-button {
            flex: 1;
            padding: 0.75rem 1rem;
            background: transparent;
            border: none;
            border-radius: 12px;
            color: #86868b;
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 600;
            font-size: 0.9rem;
            text-align: center;
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
        }

        .tab-button:hover {
            color: #1d1d1f;
        }

        .tab-button:active {
            transform: scale(0.96);
        }

        .tab-button.active {
            background: white;
            color: #007aff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        /* Modern Card Style */
        .settings-card {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            padding: 1.5rem;
            border-radius: 20px;
            border: 0.5px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
        }

        /* Mobile Responsive Styles */
        @media (max-width: 768px) {
            nav {
                padding: 0.75rem 1rem;
            }

            nav h1 {
                font-size: 1.1rem;
            }

            nav .nav-links {
                gap: 0.4rem;
            }

            nav a {
                padding: 0.4rem 0.75rem;
                font-size: 0.85rem;
            }

            .container {
                padding: 1rem;
            }

            .server-header {
                padding: 1.25rem;
            }

            .server-header img {
                width: 52px;
                height: 52px;
            }

            .server-header h2 {
                font-size: 1.25rem;
            }

            .server-info {
                gap: 0.5rem;
            }

            .info-badge {
                font-size: 0.75rem;
                padding: 0.3rem 0.6rem;
            }

            .stats {
                grid-template-columns: repeat(2, 1fr);
                gap: 0.75rem;
            }

            .stat-card {
                padding: 1rem;
            }

            .stat-card h3 {
                font-size: 1.75rem;
            }

            .stat-card p {
                font-size: 0.75rem;
            }

            .stat-card .icon {
                font-size: 1.25rem;
            }

            .tab-navigation {
                gap: 0.4rem;
                padding: 0.4rem;
            }

            .tab-button {
                padding: 0.65rem 0.5rem;
                font-size: 0.8rem;
            }

            .settings-section, .cases-section, .settings-card {
                padding: 1.25rem;
                margin-bottom: 1rem;
            }

            .settings-section h2, .cases-section h2 {
                font-size: 1.1rem;
            }

            .form-group {
                margin-bottom: 1rem;
            }

            .form-group label {
                font-size: 0.85rem;
            }

            .form-group select {
                padding: 0.65rem;
                font-size: 0.9rem;
            }

            .checkbox-group {
                gap: 0.6rem;
            }

            .checkbox-item {
                padding: 0.6rem 0.75rem;
            }

            .checkbox-item label {
                font-size: 0.85rem;
            }

            .btn {
                width: 100%;
                padding: 0.85rem;
                font-size: 0.9rem;
            }

            .case-item {
                padding: 0.85rem;
            }

            .case-item h4 {
                font-size: 0.95rem;
            }

            .case-item p {
                font-size: 0.8rem;
            }

            .quick-info {
                grid-template-columns: 1fr;
                gap: 0.6rem;
            }

            .info-card {
                padding: 0.85rem;
            }

            .info-card h4 {
                font-size: 0.7rem;
            }

            .info-card p {
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .server-header h2 {
                font-size: 1.1rem;
            }

            .stats {
                grid-template-columns: 1fr;
            }

            .stat-card h3 {
                font-size: 1.5rem;
            }

            .settings-section h2, .cases-section h2 {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <nav>
        <h1>🛡️ Oblivion Bot Dashboard</h1>
        <div class="nav-links">
            <a href="/dashboard">← Servers</a>
            <a href="/">Home</a>
            <a href="/logout">Logout</a>
        </div>
    </nav>

    <div class="container">
        <div class="server-header">
            <img src="<%= guild.iconURL() || 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="<%= guild.name %>">
            <div>
                <h2><%= guild.name %></h2>
                <div class="server-info">
                    <span class="info-badge">👥 <%= guild.memberCount %> Members</span>
                    <span class="info-badge">💬 <%= guild.channels.cache.filter(c => c.type === 0).size %> Text Channels</span>
                    <span class="info-badge">🔊 <%= guild.channels.cache.filter(c => c.type === 2).size %> Voice Channels</span>
                    <span class="info-badge">🎭 <%= guild.roles.cache.size %> Roles</span>
                </div>
            </div>
        </div>

        <div class="stats">
            <div class="stat-card">
                <div class="icon">📋</div>
                <h3><%= caseStats.total %></h3>
                <p>Total Cases</p>
            </div>
            <div class="stat-card">
                <div class="icon">🔨</div>
                <h3><%= caseStats.bans %></h3>
                <p>Bans</p>
            </div>
            <div class="stat-card">
                <div class="icon">👢</div>
                <h3><%= caseStats.kicks %></h3>
                <p>Kicks</p>
            </div>
            <div class="stat-card">
                <div class="icon">🔇</div>
                <h3><%= caseStats.mutes %></h3>
                <p>Mutes</p>
            </div>
            <div class="stat-card">
                <div class="icon">⚠️</div>
                <h3><%= caseStats.warns %></h3>
                <p>Warnings</p>
            </div>
        </div>

        <div class="settings-section">
            <h2>⚙️ Server Configuration</h2>
            <div class="success-message" id="successMessage">✅ Settings saved successfully!</div>
            <div class="error-message" id="errorMessage">❌ Failed to save settings.</div>
            
            <!-- Tab Navigation -->
            <div class="tab-navigation">
                <div class="tab-button active" onclick="switchTab('general')">📋 General</div>
                <div class="tab-button" onclick="switchTab('moderation')">🛡️ Moderation</div>
                <div class="tab-button" onclick="switchTab('leveling')">📊 XP & Levels</div>
                <div class="tab-button" onclick="switchTab('achievements')">🏆 Achievements</div>
            </div>

            <form id="settingsForm">
                <!-- General Tab -->
                <div id="general-tab" class="tab-content active settings-card">
                    <h3 style="color: #667eea; margin-bottom: 1.5rem; font-size: 1.3rem;">📋 General Settings</h3>
                    
                <div class="form-group">
                    <label for="modLogChannel">📋 Mod Log Channel</label>
                    <p style="color: #666; font-size: 0.85rem; margin-bottom: 0.5rem;">Choose a channel where all moderation actions will be logged</p>
                    <select id="modLogChannel" name="modLogChannel">
                        <option value="">None</option>
                        <% channels.forEach(channel => { %>
                            <option value="<%= channel.id %>" <%= settings.mod_log_channel === channel.id ? 'selected' : '' %>>
                                #<%= channel.name %>
                            </option>
                        <% }); %>
                    </select>
                </div>

                <div class="form-group">
                    <label for="oblivionLogChannel">📜 Oblivion Log Channel</label>
                    <p style="color: #666; font-size: 0.85rem; margin-bottom: 0.5rem;">Track EVERYTHING: all messages, edits, deletions, channels, roles, emojis, stickers, and more</p>
                    <select id="oblivionLogChannel" name="oblivionLogChannel">
                        <option value="">None</option>
                        <% channels.forEach(channel => { %>
                            <option value="<%= channel.id %>" <%= settings.oblivion_log_channel === channel.id ? 'selected' : '' %>>
                                #<%= channel.name %>
                            </option>
                        <% }); %>
                    </select>
                </div>
                </div>

                <!-- Moderation Tab -->
                <div id="moderation-tab" class="tab-content settings-card">
                    <h3 style="color: #667eea; margin-bottom: 1.5rem; font-size: 1.3rem;">🛡️ Moderation & AutoMod</h3>

                <div class="form-group">
                    <label>🤖 AutoMod Features</label>
                    <p style="color: #666; font-size: 0.85rem; margin-bottom: 1rem;">Configure automatic moderation rules and actions</p>
                    
                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; margin-bottom: 1rem; border: 1px solid rgba(255,255,255,0.1);">
                        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem;">
                            <div>
                                <input type="checkbox" id="antiSpam" name="antiSpam" <%= settings.automod_anti_spam ? 'checked' : '' %>>
                                <label for="antiSpam" style="margin-left: 0.5rem; font-weight: 600;">🚫 Anti-Spam</label>
                                <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 1.8rem;">Detect 5+ messages in 5 seconds</p>
                            </div>
                            <select id="antiSpamAction" name="antiSpamAction" style="width: 200px; padding: 0.5rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                                <option value="delete" <%= (settings.automod_anti_spam_action || 'delete') === 'delete' ? 'selected' : '' %>>Delete Message</option>
                                <option value="warn" <%= settings.automod_anti_spam_action === 'warn' ? 'selected' : '' %>>Warn Member</option>
                                <option value="both" <%= settings.automod_anti_spam_action === 'both' ? 'selected' : '' %>>Delete Message & Warn Member</option>
                            </select>
                        </div>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; margin-bottom: 1rem; border: 1px solid rgba(255,255,255,0.1);">
                        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem;">
                            <div>
                                <input type="checkbox" id="antiInvite" name="antiInvite" <%= settings.automod_anti_invite ? 'checked' : '' %>>
                                <label for="antiInvite" style="margin-left: 0.5rem; font-weight: 600;">🔗 Server Invites</label>
                                <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 1.8rem;">Block Discord server invites</p>
                            </div>
                            <select id="antiInviteAction" name="antiInviteAction" style="width: 200px; padding: 0.5rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                                <option value="delete" <%= (settings.automod_anti_invite_action || 'delete') === 'delete' ? 'selected' : '' %>>Delete Message</option>
                                <option value="warn" <%= settings.automod_anti_invite_action === 'warn' ? 'selected' : '' %>>Warn Member</option>
                                <option value="both" <%= settings.automod_anti_invite_action === 'both' ? 'selected' : '' %>>Delete Message & Warn Member</option>
                            </select>
                        </div>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; margin-bottom: 1rem; border: 1px solid rgba(255,255,255,0.1);">
                        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem;">
                            <div>
                                <input type="checkbox" id="antiLink" name="antiLink" <%= settings.automod_anti_link ? 'checked' : '' %>>
                                <label for="antiLink" style="margin-left: 0.5rem; font-weight: 600;">🌐 External Links</label>
                                <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 1.8rem;">Block external website links</p>
                            </div>
                            <select id="antiLinkAction" name="antiLinkAction" style="width: 200px; padding: 0.5rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                                <option value="delete" <%= (settings.automod_anti_link_action || 'delete') === 'delete' ? 'selected' : '' %>>Delete Message</option>
                                <option value="warn" <%= settings.automod_anti_link_action === 'warn' ? 'selected' : '' %>>Warn Member</option>
                                <option value="both" <%= settings.automod_anti_link_action === 'both' ? 'selected' : '' %>>Delete Message & Warn Member</option>
                            </select>
                        </div>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem;">
                            <div>
                                <label style="font-weight: 600;">🤬 Bad Words</label>
                                <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 0;">Filter banned words and phrases</p>
                            </div>
                            <select id="bannedWordsAction" name="bannedWordsAction" style="width: 200px; padding: 0.5rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                                <option value="delete" <%= (settings.automod_banned_words_action || 'delete') === 'delete' ? 'selected' : '' %>>Delete Message</option>
                                <option value="warn" <%= settings.automod_banned_words_action === 'warn' ? 'selected' : '' %>>Warn Member</option>
                                <option value="both" <%= settings.automod_banned_words_action === 'both' ? 'selected' : '' %>>Delete Message & Warn Member</option>
                            </select>
                        </div>
                        <p style="color: #888; font-size: 0.85rem; margin-top: 1rem;">Use <code style="background: rgba(0,0,0,0.3); padding: 2px 6px; border-radius: 3px;">/settings banned-words add</code> to add words</p>
                    </div>
                </div>
                </div>

                <!-- Leveling Tab -->
                <div id="leveling-tab" class="tab-content settings-card">
                    <h3 style="color: #667eea; margin-bottom: 1.5rem; font-size: 1.3rem;">📊 XP & Leveling System</h3>

                <div class="form-group">
                    <label>📊 XP & Leveling</label>
                    <p style="color: #666; font-size: 0.85rem; margin-bottom: 1rem;">Configure XP and leveling system</p>
                    
                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <div>
                            <input type="checkbox" id="levelUpMessages" name="levelUpMessages" <%= !settings || settings.level_up_messages === undefined || settings.level_up_messages === 1 ? 'checked' : '' %>>
                            <label for="levelUpMessages" style="margin-left: 0.5rem; font-weight: 600;">🎉 Level Up Messages</label>
                            <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 1.8rem;">Show announcements when users level up</p>
                        </div>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1); margin-top: 1rem;">
                        <div>
                            <input type="checkbox" id="achievementMessages" name="achievementMessages" <%= !settings || settings.achievement_messages === undefined || settings.achievement_messages === 1 ? 'checked' : '' %>>
                            <label for="achievementMessages" style="margin-left: 0.5rem; font-weight: 600;">🏆 Achievement Messages</label>
                            <p style="color: #888; font-size: 0.85rem; margin: 0.25rem 0 0 1.8rem;">Show announcements when users unlock achievements (messages, voice, reactions)</p>
                        </div>
                    </div>
                </div>
                </div>

                <!-- Achievements Tab -->
                <div id="achievements-tab" class="tab-content settings-card">
                    <h3 style="color: #667eea; margin-bottom: 1.5rem; font-size: 1.3rem;">🏆 Achievement Role Rewards</h3>

                <h3 style="margin: 2rem 0 1rem 0; color: #667eea; display: flex; align-items: center; gap: 0.5rem;">
                    🏆 Achievement Role Rewards
                </h3>

                <div style="display: grid; gap: 1rem;">
                    <!-- Message Achievements -->
                    <h4 style="color: #667eea; margin: 1rem 0 0.5rem 0;">📨 Message Achievements</h4>
                    
                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">💬 100 Messages - Newbie Chatter</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gray tier achievement</p>
                        <select id="msg100Role" name="msg100Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.msg_100_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">📨 500 Messages - Active Chatter 🥉</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Bronze tier achievement</p>
                        <select id="msg500Role" name="msg500Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.msg_500_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">📬 1,000 Messages - Dedicated Chatter 🥈</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Silver tier achievement</p>
                        <select id="msg1000Role" name="msg1000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.msg_1000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">📮 5,000 Messages - Elite Chatter 🥇</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gold tier achievement</p>
                        <select id="msg5000Role" name="msg5000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.msg_5000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">💎 10,000 Messages - Legendary Chatter 💎</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Diamond tier achievement</p>
                        <select id="msg10000Role" name="msg10000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.msg_10000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <!-- Voice Achievements -->
                    <h4 style="color: #667eea; margin: 1.5rem 0 0.5rem 0;">🎤 Voice Achievements</h4>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🎙️ 30 Minutes - Voice Newbie</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gray tier achievement</p>
                        <select id="vc30Role" name="vc30Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.vc_30_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🎤 60 Minutes - Voice Regular 🥉</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Bronze tier achievement (1 hour)</p>
                        <select id="vc60Role" name="vc60Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.vc_60_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🔊 500 Minutes - Voice Enthusiast 🥈</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Silver tier achievement (8 hours)</p>
                        <select id="vc500Role" name="vc500Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.vc_500_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">📢 1,000 Minutes - Voice Expert 🥇</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gold tier achievement (16 hours)</p>
                        <select id="vc1000Role" name="vc1000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.vc_1000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🎵 5,000 Minutes - Voice Legend 💎</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Diamond tier achievement (83 hours)</p>
                        <select id="vc5000Role" name="vc5000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.vc_5000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <!-- Reaction Achievements -->
                    <h4 style="color: #667eea; margin: 1.5rem 0 0.5rem 0;">👍 Reaction Achievements</h4>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">👍 50 Reactions - Reactor 🥉</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Bronze tier - Reactions given to others</p>
                        <select id="react50Role" name="react50Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.react_50_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">⭐ 250 Reactions - Super Reactor 🥈</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Silver tier - Reactions given to others</p>
                        <select id="react250Role" name="react250Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.react_250_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🌟 1,000 Reactions - Mega Reactor 🥇</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gold tier - Reactions given to others</p>
                        <select id="react1000Role" name="react1000Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.react_1000_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <!-- Popularity Achievements -->
                    <h4 style="color: #667eea; margin: 1.5rem 0 0.5rem 0;">✨ Popularity Achievements</h4>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">✨ 100 Reactions - Rising Star 🥈</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Silver tier - Reactions received on your messages</p>
                        <select id="popular100Role" name="popular100Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.popular_100_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">
                        <label style="display: block; margin-bottom: 0.5rem; font-weight: 600;">🌠 500 Reactions - Superstar 🥇</label>
                        <p style="color: #888; font-size: 0.85rem; margin-bottom: 0.75rem;">Gold tier - Reactions received on your messages</p>
                        <select id="popular500Role" name="popular500Role" style="width: 100%; padding: 0.75rem; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white;">
                            <option value="">No Role</option>
                            <% roles.forEach(role => { %>
                                <% if (role.id !== guild.id) { %>
                                    <option value="<%= role.id %>" <%= achievementSettings && achievementSettings.popular_500_role === role.id ? 'selected' : '' %>>
                                        <%= role.name %>
                                    </option>
                                <% } %>
                            <% }); %>
                        </select>
                    </div>

                    <div style="background: rgba(102, 126, 234, 0.1); padding: 1rem; border-radius: 6px; border-left: 4px solid #667eea;">
                        <p style="color: #888; font-size: 0.85rem; margin: 0;">💡 <strong>Note:</strong> When a user earns a higher tier achievement, the previous tier's role will be automatically removed. Use <code style="background: rgba(0,0,0,0.3); padding: 2px 6px; border-radius: 3px;">/achsetup</code> to auto-create all 15 achievement roles!</p>
                    </div>
                </div>
                </div>

                <button type="submit" class="btn">💾 Save Settings</button>
            </form>
        </div>

        <div class="cases-section">
            <h2>📋 Recent Moderation Cases</h2>
            <% if (recentCases.length === 0) { %>
                <div style="text-align: center; padding: 3rem; color: #666;">
                    <div style="font-size: 3rem; margin-bottom: 1rem;">📭</div>
                    <p style="font-size: 1.1rem;">No moderation cases yet.</p>
                    <p style="font-size: 0.9rem; margin-top: 0.5rem;">Cases will appear here when you use moderation commands.</p>
                </div>
            <% } else { %>
                <% recentCases.forEach(modCase => { %>
                    <div class="case-item">
                        <h4>Case #<%= modCase.case_number %> — <%= modCase.action %></h4>
                        <p><strong>👤 User:</strong> <%= modCase.user_tag %></p>
                        <p><strong>👮 Moderator:</strong> <%= modCase.moderator_tag %></p>
                        <p><strong>📝 Reason:</strong> <%= modCase.reason %></p>
                        <p><strong>📅 Date:</strong> <%= new Date(modCase.created_at).toLocaleString() %></p>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>

    <script>
        document.getElementById('settingsForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const formData = {
                modLogChannel: document.getElementById('modLogChannel').value,
                oblivionLogChannel: document.getElementById('oblivionLogChannel').value,
                antiSpam: document.getElementById('antiSpam').checked,
                antiInvite: document.getElementById('antiInvite').checked,
                antiLink: document.getElementById('antiLink').checked,
                antiSpamAction: document.getElementById('antiSpamAction').value,
                antiInviteAction: document.getElementById('antiInviteAction').value,
                antiLinkAction: document.getElementById('antiLinkAction').value,
                bannedWordsAction: document.getElementById('bannedWordsAction').value,
                levelUpMessages: document.getElementById('levelUpMessages').checked,
                achievementMessages: document.getElementById('achievementMessages').checked,
                // Message Achievements
                msg100Role: document.getElementById('msg100Role').value,
                msg500Role: document.getElementById('msg500Role').value,
                msg1000Role: document.getElementById('msg1000Role').value,
                msg5000Role: document.getElementById('msg5000Role').value,
                msg10000Role: document.getElementById('msg10000Role').value,
                // Voice Achievements
                vc30Role: document.getElementById('vc30Role').value,
                vc60Role: document.getElementById('vc60Role').value,
                vc500Role: document.getElementById('vc500Role').value,
                vc1000Role: document.getElementById('vc1000Role').value,
                vc5000Role: document.getElementById('vc5000Role').value,
                // Reaction Achievements
                react50Role: document.getElementById('react50Role').value,
                react250Role: document.getElementById('react250Role').value,
                react1000Role: document.getElementById('react1000Role').value,
                // Popularity Achievements
                popular100Role: document.getElementById('popular100Role').value,
                popular500Role: document.getElementById('popular500Role').value
            };

            try {
                const response = await fetch('/api/guild/<%= guild.id %>/settings', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    const successMsg = document.getElementById('successMessage');
                    successMsg.style.display = 'block';
                    setTimeout(() => successMsg.style.display = 'none', 3000);
                } else {
                    const errorData = await response.json();
                    console.error('Server error:', errorData);
                    throw new Error(errorData.error || 'Failed to save');
                }
            } catch (error) {
                console.error('Error saving settings:', error);
                const errorMsg = document.getElementById('errorMessage');
                errorMsg.textContent = `❌ Error: ${error.message}`;
                errorMsg.style.display = 'block';
                setTimeout(() => errorMsg.style.display = 'none', 5000);
            }
        });

        // Tab switching function
        function switchTab(tabName) {
            // Hide all tabs
            const tabs = document.querySelectorAll('.tab-content');
            tabs.forEach(tab => tab.classList.remove('active'));
            
            // Remove active class from all buttons
            const buttons = document.querySelectorAll('.tab-button');
            buttons.forEach(btn => btn.classList.remove('active'));
            
            // Show selected tab
            document.getElementById(tabName + '-tab').classList.add('active');
            
            // Add active class to clicked button
            event.target.classList.add('active');
        }
    </script>
</body>
</html>
